import { DynamicScriptDirective } from './dynamic-script.directive';

describe('DynamicScriptDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicScriptDirective();
    expect(directive).toBeTruthy();
  });
});
